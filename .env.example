# OneNet 推送配置（在 OneNet 控制台创建 HTTP 推送实例时填写相同 Token）
ONENET_TOKEN=your_onenet_token_here

# 是否跳过 Token 签名校验（仅开发调试用，生产环境请设为 false）
ONENET_SKIP_VERIFY=false

# PostgreSQL 连接（docker-compose 默认配置）
DATABASE_URL=postgresql+asyncpg://onenet:onenet_secret@db:5432/onenet_db

# 服务端口
APP_PORT=8000
